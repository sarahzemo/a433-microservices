apiVersion: v1
kind: Service
metadata:
  name: karsajobs-service
spec:
  type: NodePort
  selector:
    app: karsajobs
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      nodePort: 30007
